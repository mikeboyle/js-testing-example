import React from "react";
import { render } from "@testing-library/react";
import WordCounterResult from "../index";

/*
 * Write two tests of the WordCounterResult component.
 * Think about what component behavior needs to be tested.
 * How does the output change, depending on the props?
 */
